/*import 'package:built_collection/built_collection.dart';

extension BuiltListExtension<E> on BuiltList<E> {
  E get firstOrNull => this.isEmpty ? null : this.first;

  E firstWhereOrNull(bool Function(E) test) {
    final index = this.indexWhere(test);
    if (index == -1) {
      return null;
    }
    return this[index];
  }

  E get lastOrNull => this.isEmpty ? null : this.last;
}*/

